# Row-review paper-path correction

The frozen `/tmp/clamsara-variant-row-review-bnum_11t/review-snapshot.json`
contains an incorrect `paper_root` value:
`/tmp/clamsara-description-review-7p14doks/paper-v14` does not exist.
The frozen manifest and original reports have not been edited.

The parent read the normative paper at
`/home/iskam/src/vibe/Clamsara/paper-v14` and checked its files against the
previous paper hashes. Those files remain unchanged. That parent check must
not be described as a fresh independent normative-paper review.

The row reviewer explicitly clarified that the whole-row proposal and repaired
row source reviews used the supplied contract/proposal, frozen implementation
and tests, and earlier paper-reference context. The reviewer did **not** freshly
read the normative paper at another path, nor verify the erroneous `paper_root`.
The earlier clients.tex:128–136 reference is not evidence of a fresh direct
paper read during these row reviews.

Thus the independent row verdict remains exactly scoped: source reasoning and
120 passing native cases for the proposed serialized hosted row-closure policy.
Its 41 source/19 overlay hashes and native results are unaffected. It is not an
independent paper-conformance audit. The parent full/stress evidence remains
separately attributed. No full conformance, benchmark or target acceptance
follows from this repair.

The subsequent indexed/name reviewer has been authorized to read the actual
normative path above and records those paths and hashes independently. That
later read does not retroactively supply evidence for the earlier reviews.
