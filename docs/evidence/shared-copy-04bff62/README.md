# Shared copying action evidence (baseline 04bff62 / runtime 1099)

This is a behavior-preserving private implementation refactor, NOT full protocol,
collector-set, managed-finalizer, storage-accounting, benchmark or target acceptance.

- `regression-source/`: unchanged independent source draft and its original
  unexecuted/source-only provenance. Later native results are separate below.
- `candidate-source/`: exact two-file patch, design, input/output hashes and
  independent ClosedRouter Flash source review. The original full Git archive is
  not duplicated here: recover baseline commit04bff62 from Git. Its historical
  archive hash/path remains in the original provenance record.
- `baseline-16/` and `candidate-16/`: unchanged16-case native probes, both exit0.
  Four successful graph configurations complete normally. All16 application
  root-provider tokens remain registered; do not claim complete client-owner
  discharge. Twelve injected-failure worlds remain published and rooted: six
  reopen after reversible first-copy failure, six keep a covered stop after an
  earlier publication. Retention lasts through the process summary, not after exit.
- `core-run-01-failed/`: main/tools/gen/288 histories passed; the parent runner
  incorrectly requested workload structural inspection without loading workload.
- `core-run-02-failed/`: those suites plus core structure passed; the parent's
  explicit generational closure checker omitted the existing project whitelist
  for the standard INITIALIZE-INSTANCE method. This is a runner failure.
- `core-run-03/`: unchanged production/test bytes; corrected runner uses the
  existing project check policy, with no new exemptions. All selected suites and
  core/generational structural checks pass. No workload or benchmark execution.
- `protocol-review/`: original report MUST be read with its addendum, evidence
  erratum and bridge-dispatch correction. Private helpers and duplicate code are
  not automatically protocol violations. The builder/cycle really use declared
  protocols; concrete-family coupling and host bridges remain. Parent verified
  all22 external-file digests; the pre-table self-hash is not a current file pin.

Historical runners retain their original absolute source/cache paths. They are
execution evidence, not a claim that those /tmp paths exist on another machine.
For the permanent copy-action suite, load/test :clamsara/quality/generational/test
through the normal project ASDF environment. The fixture retains expected failed
worlds deliberately; do not drain/close them to manufacture a pass.

No native result here proves partial-byte-copy faults, this object's own immediate
post-forwarding fault, raw to-space exhaustion, or Mezzano no-allocation behavior.
The shared callback is a top-level private function reference, not a new public
protocol. No Immix, Sticky Immix, Claimore, Evha or Iso implementation is supplied
by this refactor.
