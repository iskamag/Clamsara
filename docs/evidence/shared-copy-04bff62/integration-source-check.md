# Parent integration check

Exact integrated source: /tmp/clamsara-shared-copy-integrated-_nomuxyd.
Only four code/test/ASDF overlays relative to04bff62. Production two files match
independently reviewed candidate hashes. Independent test body (DEFSTRUCT through
RUN-FOCUSED-COPY-TESTS) is byte-identical, SHA256
77dd7a0fc307ab4630082e8c51393315e8b680711e157452690485afc60597d3.
Header/package/export adapt to permanent package; new wrapper follows original body.
Independent author checked this source integration and found no blocker.

The old owner list remains the tail; LDIFF checks only each new group, with16 real
worlds,4 completed configurations,12 expected failed worlds,0 unexpected. The
ASDF component follows fixtures and invokes the exported strict wrapper in AND.
Native evidence is parent execution, not an independent author's native replay.
Archive regression is loaded from the integration candidate's evidence directory.

All48 copy-case worlds were instantiated across permanent/archive/permanent-repeat
runs:12 completed configurations and36 deliberately retained failed worlds, of
which18 keep covered stops. These are copy-suite counts only, not the separate
expected-fatal worlds from existing core tests. Application root-provider tokens
remain registered even after configuration shutdown: no complete client-owner
or benchmark discharge claim. All retention claims end with the process summary.
