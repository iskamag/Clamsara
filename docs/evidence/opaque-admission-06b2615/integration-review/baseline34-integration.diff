--- native15448/acceptance-source-02.lisp
+++ installed/barrier-admission.lisp
@@ -1,12 +1,11 @@
-;;;; Source-authored bounded opaque-reservation admission checks.
-;;;; Not executed by the source-only reviewer. Load CLAMSARA/QUALITY/SUPPORT,
-;;;; then this file, then call RUN-OPAQUE-ADMISSION-TESTS explicitly.
-;;;; This tests hosted structural admission and real execution, not target entry
-;;;; admission, arbitrary effective-method correctness or allocation freedom.
-(defpackage #:clamsara.independent.opaque-admission
+;;;; Hosted opaque-reservation admission histories.
+;;;; Based on the unchanged independent 34-case baseline; no load-time runner.
+;;;; Tests structural checks and real execution, not full method-domain or
+;;;; Mezzano admission. Retained owners survive repeated test invocations.
+(defpackage #:clamsara.quality.barrier-admission
   (:use #:cl #:clamsara #:clamsara.quality.support)
   (:export #:run-opaque-admission-tests #:*failed-worlds* #:*rejected-builds*))
-(in-package #:clamsara.independent.opaque-admission)
+(in-package #:clamsara.quality.barrier-admission)
 
 (define-condition proof-fault (error) ())
 (defclass private-token ()
