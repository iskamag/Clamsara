--- failed-candidate/test/workload/mapc.lisp
+++ corrected-candidate/test/workload/mapc.lisp
@@ -368,6 +368,6 @@
     (format t "~&WORKLOAD-MAPC groups=9 passed=~D failed=~D owners=~D~%"
             passed failed (length (ldiff *owners* before)))
     (assert (zerop failed))
-    (assert (= 9 passed (length (ldiff *owners* before))))
+    (assert (= 9 passed (length (ldiff *owners* before)))))
   t)
 (export 'run-workload-mapc-tests)
